version: '3.8'

services:
  bsbm-benchmark:
    build: .
    image: bsbm-benchmark
    volumes:
      - ./output:/app/output
    environment:
      - JAVA_OPTS=-Xmx2G
    # Command is passed via docker compose run, default removed to allow override

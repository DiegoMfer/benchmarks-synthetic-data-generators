version: '3.8'

services:
  bsbm-benchmark:
    build: .
    image: bsbm-benchmark
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./output:/app/output
    environment:
      - JAVA_OPTS=-Xmx2G
    command: ["--products", "1000"]

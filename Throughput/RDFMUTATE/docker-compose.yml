version: '3.8'

services:
  rdfmutate-benchmark:
    build: .
    image: rdfmutate-benchmark
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./output:/app/output
    environment:
      - JAVA_OPTS=-Xmx2G
    command: ["--config", "config.yaml"]

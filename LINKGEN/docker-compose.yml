version: '3.8'

services:
  linkgen-benchmark:
    build: .
    image: linkgen-benchmark
    user: "${UID:-1000}:${GID:-1000}"
    volumes:
      - ./output:/app/output
    environment:
      - JAVA_OPTS=-Xmx4G
    # Command is passed via docker compose run, default removed to allow override
